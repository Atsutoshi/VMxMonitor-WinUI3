<ContentDialog
    x:Class="VMxMonitor.dialogs.AuthenticationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VMxMonitor.dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Title="{Binding titleAuthenticationError, Source={StaticResource resx}}"
    PrimaryButtonText="Reconfirm"
    SecondaryButtonText="Quit">

    <StackPanel Margin="20">
        <TextBlock x:Name="wMessageText" FontSize="18" TextWrapping="Wrap" Text="{Binding errorApplicationAuthentication, Mode=OneWay, Source={StaticResource resx}}" Margin="0,0,0,8" VerticalAlignment="Top" HorizontalAlignment="Left"/>
    </StackPanel>
</ContentDialog>
